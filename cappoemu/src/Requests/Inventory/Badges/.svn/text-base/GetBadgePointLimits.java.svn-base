package Requests.Inventory.Badges;

import Server.Connection;
import Requests.Handler;
import Server.Server;

/*
 *****************
 * @author capos *
 *****************
*/

public class GetBadgePointLimits extends Handler
{
    @Override
    public void ParseIn(Connection Main, Server Environment)
    {
        // pending..


        /* AchievementsScoreEvent
        Environment.InitPacket(443,true);
        Environment.AppendInt(Main.AchievementsScore);
        Environment.EndPacket();
        Main.SendData();*/
    }
}
